import 'package:flutter/material.dart';

class OpcionSecond extends StatelessWidget {
  const OpcionSecond({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("OPCION 2"),
      ),
    );
  }
}
